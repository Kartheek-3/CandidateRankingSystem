# Copy this file to .env and fill in your values
# The system works without GOOGLE_API_KEY (falls back to heuristic pipeline)

# Optional: Groq API key for LLM-powered agents (Stages 1, 3, 4)
# Get one free at https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
